package org.dd4t;

/**
 * The {@code dxa-compatible} artifact contains a full copy of the latest compatible DD4T.
 * It is a full copy of it without any modification, and supposed to work as pure DD4T.
 * <p>All the DD4T artifacts are flattened into the single artifact saving the package structure though.</p>
 *
 * @dxa.publicApi This class is intentionally left empty, and is needed for the Public API documentation generation.
 */
@SuppressWarnings("unused")
public class DxaCompatibleEntryPoint {
    // empty
}
