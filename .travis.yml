language: java
sudo: false
jdk:
  - openjdk8
install: 
  - skip
os: 
  - linux
dist: 
  - trusty

script:
  - git update-index --chmod=+x gradlew
  - chmod +x gradlew
  - git ls-tree HEAD
  - ./gradlew build
